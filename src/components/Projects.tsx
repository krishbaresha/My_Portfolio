/**
 * Projects — thin wrapper kept for backward-compat imports.
 * The actual rendering is done by BentoGrid which receives
 * server-fetched project data from page.tsx.
 */
export { default } from './BentoGrid';
